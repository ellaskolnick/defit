<div class="container">
  <h1><%= @product.name %></h1>

  <h3>Rent</h3>
  <%= simple_form_for [@product, @booking] do |f| %>
    <%= f.input :start_date %>
    <%= f.input :end_date %>
    <%= f.button :submit%>
  <% end %>

  <%= link_to 'Back', products_path %>
</div>
